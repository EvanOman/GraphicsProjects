#ifndef CIRCLEINCLUDE_H
#define CIRCLEINCLUDE_H

#include "glinclude.h"
#include <vector>

std::vector<glm::vec4> circlePositions();

std::vector<glm::vec4> circleNormals();

std::vector<glm::vec4> circleColors();

std::vector<int> circleIndices();

#endif